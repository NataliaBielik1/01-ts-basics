const name: { name: string } = "Alice";
const age: {age: number }= 30;
const isOnline: {isOnline: boolean }= true;

console.log(`Name: ${name}`);
console.log(`Age: ${age}`);
console.log(`Online: ${isOnline}`);